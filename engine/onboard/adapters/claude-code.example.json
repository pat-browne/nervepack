{
  "host": "claude-code",
  "comment": "Reference adapter manifest produced by onboarding nervepack to Claude Code. Copy to ~/.config/nervepack/adapter.json (per-machine; not committed). Each 'verify' is a deterministic shell command the doctor runs (exit 0 = the capability is genuinely wired). The wiring it verifies is what `cli.py setup link-skills` and setup/5x-install-*.sh produce \u2014 read those as the worked example.",
  "paths": {
    "settings": "~/.claude/settings.json",
    "skills_dir": "~/.claude/skills",
    "transcripts": "~/.claude/projects"
  },
  "paths_note": "OPTIONAL, and optional per key. Omit the block, or any single key, and nervepack uses its built-in ~/.claude/... default for whatever is missing -- every manifest written before #300 has no paths block and keeps working unchanged. The matching environment variable (CLAUDE_SETTINGS / NP_SKILLS_DST / CLAUDE_PROJECTS_DIR) still wins over both. A relative value is ignored and reported by the doctor.",
  "capabilities": {
    "knowledge": {
      "status": "wired",
      "verify": "test -e ~/.claude/skills/np-core-sync/SKILL.md"
    },
    "session-start": {
      "status": "wired",
      "verify": "grep -q nervepack-session-directive ~/.claude/settings.json"
    },
    "session-end-capture": {
      "status": "wired",
      "verify": "grep -q episodic-capture ~/.claude/settings.json"
    },
    "session-end-flush": {
      "status": "wired",
      "verify": "grep -q np-session-flush ~/.claude/settings.json"
    },
    "scheduled-maint": {
      "status": "wired",
      "verify": "crontab -l 2>/dev/null | grep -q nervepack"
    }
  }
}
