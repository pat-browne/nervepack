# Python bytecode caches (harness Python scripts are stdlib-only; no build artifacts)
__pycache__/
*.pyc

# superpowers visual-companion brainstorm scratch
.superpowers/
/dashboard/data

# Subagent worktree scratch (transient)
.claude/
.worktrees/

# Temporary CI debug artifact (see test_implement.sh) -- remove alongside it
/.ci-debug-implement-progress.log
